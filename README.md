<!-- # DSA Practice Repository

This repository is a personal C++ practice workspace focused on core programming and Data Structures & Algorithms (DSA) basics.

It is organized as standalone practice files. Most files contain multiple exercises, often with older attempts kept as commented code.

## Who This Repo Is For

- New developers learning C++ fundamentals
- Students practicing basic DSA and problem-solving patterns
- Anyone who wants small, focused examples instead of a large framework project
**
## Repository Layout

Main practice files:

- `1_Loop.cpp`: Loop exercises (AP/GP, while/for loops, formatting output)
- `2_control_statement.cpp`: Conditional logic and control flow questions
- `3_Pattern.cpp`: Pattern-printing exercises
- `4_function.cpp`: Basic function-based problems
- `5_Pointer.cpp`: Pointer basics and swap/address examples
- `6_Array.cpp`: Array/vector practice and related problems
- `8_Array2D.cpp`: 2D array concepts and traversal/input problems
- `9_String.cpp`: String fundamentals and operations
- `10_Bubble_Sort.cpp`: Placeholder for bubble sort (currently empty)
- `leetcode.cpp`: Scratch notes for interview-style problems (currently minimal)
- `practice.cpp`, `practice1.cpp`, `pracice2.cpp`, `random.cpp`: Mixed practice and experiments

Other files:

- `305.html`: HTML practice/demo file
- `abc.tex`: LaTeX practice file
- `github.md`: Profile-style markdown content (not project documentation)

Generated artifacts:

- `*.exe` files are compiled outputs and can be regenerated locally
- `tempCodeRunnerFile.cpp` is editor-generated scratch code
**
## Prerequisites

On Windows:

- GCC/G++ from MinGW (or equivalent)
- VS Code with C/C++ extension (recommended)

## Build and Run

### Option 1: VS Code Task (already configured)

A build task exists in this workspace:

- **Task name**: `C/C++: gcc.exe build active file`
- It builds the currently open file and creates an `.exe` with the same base name

Typical flow:

1. Open any `.cpp` file.
2. Run the build task.
3. Execute the generated `.exe` from terminal.

### Option 2: Command Line (manual)

From repo root, compile and run a file:

```powershell
g++ .\6_Array.cpp -o .\6_Array.exe
.\6_Array.exe
```

Use any file name you want to run.

## Important Notes for New Developers

- Many files contain **multiple exercises** in one file.
- Older code is often commented out with `TODO` sections.
- Keep only one active `main()` at a time in a file when compiling.
- Some files are intentionally incomplete placeholders.

## Suggested Workflow

1. Pick one topic file (for example `2_control_statement.cpp`).
2. Uncomment or write one exercise.
3. Compile and run.
4. Keep previous attempts commented for reference.
5. Move to next topic.

## Potential Improvements

- Split each exercise into separate files/folders (for cleaner compilation)
- Add one short problem statement at the top of each active solution
- Add expected input/output examples in comments
- Add `.gitignore` for build artifacts like `*.exe`

## Quick Start for a New Contributor

1. Read this README.
2. Start with `1_Loop.cpp` and `2_control_statement.cpp`.
3. Build one file at a time.
4. Avoid editing generated `.exe` files.
5. Open a pull request with focused, topic-wise changes.
 -->



# 🚀 DSA Practice Repository (C++)

This repository documents my structured journey of learning **Data Structures & Algorithms (DSA)** using C++.
It contains topic-wise practice, problem-solving approaches, and foundational concepts for coding interviews.

---

## 📚 Learning Roadmap

This repository follows a step-by-step progression:

1. **Basics**

   * Loops
   * Conditionals
   * Functions
2. **Core Concepts**

   * Arrays
   * Strings
   * Pointers
3. **Intermediate**

   * 2D Arrays
   * Sorting Algorithms
4. **Problem Solving**

   * LeetCode-style questions
5. **Advanced (Upcoming)**

   * Recursion
   * Linked List
   * Stack & Queue
   * Trees & Graphs

---

## Repository Layout

Main practice files:

- `1_Loop.cpp`: Loop exercises (AP/GP, while/for loops, formatting output)
- `2_control_statement.cpp`: Conditional logic and control flow questions
- `3_Pattern.cpp`: Pattern-printing exercises
- `4_function.cpp`: Basic function-based problems
- `5_Pointer.cpp`: Pointer basics and swap/address examples
- `6_Array.cpp`: Array/vector practice and related problems
- `8_Array2D.cpp`: 2D array concepts and traversal/input problems
- `9_String.cpp`: String fundamentals and operations
- `10_Bubble_Sort.cpp`: Placeholder for bubble sort (currently empty)
- `leetcode.cpp`: Scratch notes for interview-style problems (currently minimal)
- `practice.cpp`, `practice1.cpp`, `pracice2.cpp`, `random.cpp`: Mixed practice and experiments

Other files:

- `305.html`: HTML practice/demo file
- `abc.tex`: LaTeX practice file
- `github.md`: Profile-style markdown content (not project documentation)

Generated artifacts:

- `*.exe` files are compiled outputs and can be regenerated locally
- `tempCodeRunnerFile.cpp` is editor-generated scratch code

---

## 🧠 LeetCode Practice

* Container With Most Water (Two Pointer)
* Two Sum
* Valid Parentheses
* (More coming soon...)

---

## 📈 Progress Tracker

* [x] Loops
* [x] Conditionals
* [x] Patterns
* [x] Functions
* [x] Arrays
* [x] Strings
* [ ] Sorting
* [ ] Recursion
* [ ] Linked List
* [ ] Trees & Graphs

---

## Prerequisites

On Windows:

- GCC/G++ from MinGW (or equivalent)
- VS Code with C/C++ extension (recommended)

## Build and Run

### Option 1: VS Code Task (already configured)

A build task exists in this workspace:

- **Task name**: `C/C++: gcc.exe build active file`
- It builds the currently open file and creates an `.exe` with the same base name

Typical flow:

1. Open any `.cpp` file.
2. Run the build task.
3. Execute the generated `.exe` from terminal.

### Option 2: Command Line (manual)

From repo root, compile and run a file:

```powershell
g++ .\6_Array.cpp -o .\6_Array.exe
.\6_Array.exe
```

---

## 💡 Notes

* Each file may contain multiple practice problems
* Older solutions are kept as comments for revision
* Only one `main()` function should be active while compiling
* Focus is on understanding logic rather than optimization initially

---

## 🏷️ Tags

`C++` `DSA` `Beginner` `Problem Solving` `Algorithms` `Coding Practice`

---

## 🚫 .gitignore (Recommended)

```
*.exe
tempCodeRunnerFile.cpp
```

---

## 🔥 Future Improvements

* Add time and space complexity for each solution
* Add detailed problem statements
* Convert into topic-wise folders (fully modular)
* Add advanced DSA topics

---

## 🤝 Contribution

This is a personal learning repository, but suggestions and improvements are always welcome!

---

## ⭐ Final Goal

To build strong problem-solving skills and prepare for coding interviews by mastering DSA step by step.
------------

